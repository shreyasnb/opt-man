function p = plot_cost(f_store, Y, b, Y_hat, lambda)
    N = length(f_store);
    f_opt = -trace(Y*Y'*b*b') + lambda*tr

    figure
    plot(1:N, f_store);
    grid on
    title('Cost function')
    xlabel('Iteration')
    ylabel('$f(x,y)$', 'Interpreter','latex');
end